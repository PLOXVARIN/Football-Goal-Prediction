pip install scikit-lea
pip install scikit-learn
pip install scikit-learn

ใช้เงื่อนไข (distance < 50) & (angle > 30) & (angle < 150) & (speed > 50) & (skill >= 3) เพื่อกำหนดค่า goal:
ถ้าเงื่อนไขเป็นจริง → goal = 1
ถ้าเงื่อนไขเป็นเท็จ → goal = 0